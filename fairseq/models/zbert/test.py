import torch


import torch
from sklearn.decomposition import PCA
# a = torch.LongTensor([[1, 2], [3, 4]])
# b = torch.LongTensor([[2, 2], [3, 4]])
# print((a == 1) | (b == 2))

a = [1, 2, 3]
b = [4, 5, 6]
print(a + b)
a.append(b)
print(a)
